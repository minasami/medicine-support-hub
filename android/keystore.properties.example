# Copy to android/keystore.properties (gitignored) and fill in local values.
# Never commit keystore.properties or *.keystore / *.jks.
#
# storeFile is relative to the android/app/ module directory.
storeFile=release.keystore
storePassword=
keyAlias=medicine-support-hub
keyPassword=
