# Portfolio taxonomy active learning (Android 1.0.20)

Searchable portfolio pickers load options from Appwrite `platform_taxonomy`
(seeded from medicines aggregates). **+ Add new** persists globally so other
users see the value on next load. Trade names remain company-scoped.

See `docs/PLATFORM_TAXONOMY_ACTIVE_LEARNING.md`.
