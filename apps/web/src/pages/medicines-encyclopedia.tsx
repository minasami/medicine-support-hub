import { useEffect, useMemo, useState } from "react";
import { AlertCircle, BookOpen, Building2, Handshake, History, RefreshCw, Search, ShieldCheck, ShoppingBag, SlidersHorizontal, Store } from "lucide-react";
import { Alert, AlertDescription } from "@/components/ui/alert";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { useLanguage } from "@/lib/i18n";
import { usePatientAuth } from "@/lib/patient-auth";

type Medicine={canonical_id:number;name_en:string|null;name_ar:string|null;scientific_name:string|null;manufacturer:string|null;drug_class:string|null;route:string|null;category:string|null;image_url:string|null;barcode:string|null;code:string|null;current_price_egp:number|null;price_currency:string|null;min_price_egp:number|null;max_price_egp:number|null;price_observation_count:number;distinct_price_count:number;has_price_history:boolean;source_record_count:number;source_count:number;source_systems:string[];has_verified_dataset:boolean;has_company_verified_source:boolean;marketplace_offer_count:number;marketplace_seller_count:number;lowest_marketplace_price_egp:number|null;current_price_source:string|null;relevance:number;total_count:number};
type Metrics={canonical_products:number;verified_dataset_products:number;operational_catalog_products:number;products_with_price_history:number;products_with_current_price:number;manufacturers:number;scientific_names:number;drug_classes:number;routes:number;source_records_merged:number};
type Facet={facet_type:"manufacturer"|"drug_class"|"route";facet_value:string;product_count:number};
type Filters={manufacturer:string;drugClass:string;route:string;scientificName:string;minPrice:string;maxPrice:string;historyOnly:boolean;verifiedOnly:boolean;offersOnly:boolean;sort:string};
const emptyFilters:Filters={manufacturer:"",drugClass:"",route:"",scientificName:"",minPrice:"",maxPrice:"",historyOnly:false,verifiedOnly:false,offersOnly:false,sort:"relevance"};
const PAGE_SIZE=60;
const numberOrNull=(value:string)=>{const number=Number(value);return value.trim()&&Number.isFinite(number)?number:null;};
const formatPrice=(value:number|null,currency="EGP")=>value==null?null:`${Number(value).toLocaleString()} ${currency}`;
function sourceLabel(source:string){if(source==="medicines5")return"Verified dataset";if(source==="medicines2")return"Operational catalog";if(source==="medicines3")return"EgyptDwa";if(source==="company_verified")return"Verified company portfolio";return source;}

export default function MedicinesEncyclopedia(){
  const {t,language}=useLanguage();
  const {supabaseFetch}=usePatientAuth();
  const [query,setQuery]=useState("");
  const [filters,setFilters]=useState<Filters>(emptyFilters);
  const [medicines,setMedicines]=useState<Medicine[]>([]);
  const [metrics,setMetrics]=useState<Metrics|null>(null);
  const [facets,setFacets]=useState<Facet[]>([]);
  const [offset,setOffset]=useState(0);
  const [total,setTotal]=useState(0);
  const [loading,setLoading]=useState(true);
  const [error,setError]=useState<string|null>(null);
  const [filtersOpen,setFiltersOpen]=useState(true);

  async function load(nextOffset=0,nextQuery=query,nextFilters=filters){setLoading(true);setError(null);try{
    const rows=await supabaseFetch<Medicine[]>("/rest/v1/rpc/search_medicine_encyclopedia_v2",{method:"POST",body:JSON.stringify({p_query:nextQuery.trim(),p_manufacturer:nextFilters.manufacturer||null,p_drug_class:nextFilters.drugClass||null,p_route:nextFilters.route||null,p_scientific_name:nextFilters.scientificName.trim()||null,p_min_price:numberOrNull(nextFilters.minPrice),p_max_price:numberOrNull(nextFilters.maxPrice),p_has_price_history:nextFilters.historyOnly?true:null,p_verified_only:nextFilters.verifiedOnly?true:null,p_has_marketplace_offers:nextFilters.offersOnly?true:null,p_sort:nextFilters.sort,p_limit:PAGE_SIZE,p_offset:nextOffset})});
    setMedicines(rows);setOffset(nextOffset);setTotal(Number(rows[0]?.total_count||0));
  }catch(cause){setError(cause instanceof Error?cause.message:t("Could not load medicines.","تعذر تحميل الأدوية."));}finally{setLoading(false);}}

  useEffect(()=>{void load(0,"",emptyFilters);void Promise.all([
    supabaseFetch<Metrics[]>("/rest/v1/medicine_canonical_metrics_v1?select=*"),
    supabaseFetch<Facet[]>("/rest/v1/medicine_encyclopedia_facets_v2?select=facet_type,facet_value,product_count&order=facet_type.asc,product_count.desc&limit=5000")
  ]).then(([metricRows,facetRows])=>{setMetrics(metricRows[0]||null);setFacets(facetRows);}).catch(()=>{setMetrics(null);setFacets([]);});},[]);

  const manufacturers=useMemo(()=>facets.filter(f=>f.facet_type==="manufacturer").slice(0,700),[facets]);
  const drugClasses=useMemo(()=>facets.filter(f=>f.facet_type==="drug_class").slice(0,700),[facets]);
  const routes=useMemo(()=>facets.filter(f=>f.facet_type==="route"),[facets]);
  const page=Math.floor(offset/PAGE_SIZE)+1;
  const pages=Math.max(1,Math.ceil(total/PAGE_SIZE));
  const activeFilterCount=[filters.manufacturer,filters.drugClass,filters.route,filters.scientificName,filters.minPrice,filters.maxPrice].filter(Boolean).length+Number(filters.historyOnly)+Number(filters.verifiedOnly)+Number(filters.offersOnly);
  function reset(){setQuery("");setFilters(emptyFilters);void load(0,"",emptyFilters);}

  return <main className="container mx-auto max-w-7xl px-4 py-8">
    <section className="overflow-hidden rounded-3xl border bg-card shadow-sm"><div className="grid gap-8 p-6 md:p-10 lg:grid-cols-[1.2fr_.8fr] lg:items-center"><div><p className="flex items-center gap-2 text-sm font-semibold uppercase tracking-[.14em] text-primary"><BookOpen className="h-4 w-4"/>{t("Medicine search, marketplace, and collaboration engine","محرك بحث وسوق وتعاون للأدوية")}</p><h1 className="mt-4 text-4xl font-bold tracking-tight md:text-5xl">{t("One medicine record connecting evidence, prices, companies, and verified supply offers","سجل دواء واحد يربط الأدلة والأسعار والشركات وعروض الإمداد الموثقة")}</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-muted-foreground">{t("Search the deduplicated Egyptian medicine encyclopedia, compare reviewed B2B offers from licensed pharmacies, warehouses, and distributors, and contribute attributable product knowledge.","ابحث في موسوعة الأدوية المصرية الموحدة وقارن عروض التوريد المراجعة من الصيدليات والمخازن والموزعين المرخصين وساهم بمعرفة دوائية منسوبة.")}</p><div className="mt-6 flex flex-wrap gap-3"><Button asChild><a href="/marketplace"><Store className="mr-2 h-4 w-4"/>{t("Open medicine marketplace","فتح سوق الأدوية")}</a></Button><Button asChild variant="outline"><a href="/industry"><Building2 className="mr-2 h-4 w-4"/>{t("Add company products","إضافة منتجات الشركات")}</a></Button></div></div><div className="grid gap-3"><ValueCard icon={ShieldCheck} title={t("Verified enrichment","إثراء موثق")} text={t("Medicines5 and reviewed company portfolios enrich the record without erasing original sources.","تُثري بيانات medicines5 ومحافظ الشركات المراجعة السجل دون حذف المصادر الأصلية.")}/><ValueCard icon={History} title={t("Price evidence timeline","خط زمني للأسعار")} text={t("Observed prices remain traceable by source and date precision.","تظل الأسعار المرصودة قابلة للتتبع حسب المصدر ودقة التاريخ.")}/><ValueCard icon={ShoppingBag} title={t("Verified supply offers","عروض توريد موثقة")} text={t("Compare approved seller prices, minimum orders, delivery, cold-chain support, and commercial advantages.","قارن أسعار البائعين المعتمدين والحد الأدنى للطلب والتوصيل والتبريد والمزايا التجارية.")}/></div></div></section>

    <section className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-5"><Metric label={t("Canonical products","منتجات موحدة")} value={Number(metrics?.canonical_products||0)}/><Metric label={t("Verified dataset products","منتجات البيانات الموثقة")} value={Number(metrics?.verified_dataset_products||0)}/><Metric label={t("With price history","لها تاريخ أسعار")} value={Number(metrics?.products_with_price_history||0)}/><Metric label={t("Manufacturers","الشركات المصنعة")} value={Number(metrics?.manufacturers||0)}/><Metric label={t("Source records merged","سجلات مصادر مدمجة")} value={Number(metrics?.source_records_merged||0)}/></section>
    <Alert className="mt-5"><AlertDescription>{t("Encyclopedia prices are evidence candidates. Marketplace prices are seller offers and require independent licensing, quality, availability, expiry, prescription, and procurement checks before any transaction.","أسعار الموسوعة مرشحات مبنية على الأدلة، أما أسعار السوق فهي عروض بائعين وتتطلب تحققًا مستقلًا من الترخيص والجودة والتوافر والصلاحية والوصفة وضوابط الشراء قبل أي معاملة.")}</AlertDescription></Alert>

    <section className="mt-6 rounded-2xl border bg-card p-5 shadow-sm"><div className="grid gap-3 lg:grid-cols-[1fr_auto_auto]"><label className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground"/><Input className="pl-9" value={query} onChange={event=>setQuery(event.target.value)} placeholder={t("Search commercial/scientific name, manufacturer, barcode, class, or company...","ابحث بالاسم التجاري أو العلمي أو الشركة أو الباركود أو التصنيف...")} onKeyDown={event=>{if(event.key==="Enter")void load(0);}}/></label><Button onClick={()=>void load(0)} disabled={loading}><Search className="mr-2 h-4 w-4"/>{t("Search","بحث")}</Button><Button variant="outline" onClick={reset} disabled={loading}><RefreshCw className="mr-2 h-4 w-4"/>{t("Reset","إعادة ضبط")}</Button></div><div className="mt-4 flex flex-wrap items-center justify-between gap-3"><Button variant="outline" onClick={()=>setFiltersOpen(open=>!open)}><SlidersHorizontal className="mr-2 h-4 w-4"/>{t("Advanced filters","فلاتر متقدمة")}{activeFilterCount>0?` (${activeFilterCount})`:""}</Button><div className="text-sm text-muted-foreground">{loading?t("Searching...","جاري البحث..."):`${total.toLocaleString()} ${t("matching medicines","دواء مطابق")}`}</div></div>
    {filtersOpen&&<div className="mt-5 grid gap-4 rounded-xl border bg-muted/20 p-4 md:grid-cols-2 xl:grid-cols-4"><SelectField label={t("Manufacturer","الشركة المصنعة")} value={filters.manufacturer} onChange={value=>setFilters(current=>({...current,manufacturer:value}))} options={manufacturers} emptyLabel={t("All manufacturers","كل الشركات")}/><SelectField label={t("Drug class","التصنيف الدوائي")} value={filters.drugClass} onChange={value=>setFilters(current=>({...current,drugClass:value}))} options={drugClasses} emptyLabel={t("All classes","كل التصنيفات")}/><SelectField label={t("Route","طريقة الاستخدام")} value={filters.route} onChange={value=>setFilters(current=>({...current,route:value}))} options={routes} emptyLabel={t("All routes","كل طرق الاستخدام")}/><div><Label>{t("Scientific name contains","الاسم العلمي يحتوي على")}</Label><Input className="mt-1" value={filters.scientificName} onChange={event=>setFilters(current=>({...current,scientificName:event.target.value}))}/></div><div><Label>{t("Minimum price (EGP)","أقل سعر (جنيه)")}</Label><Input className="mt-1" inputMode="decimal" value={filters.minPrice} onChange={event=>setFilters(current=>({...current,minPrice:event.target.value}))}/></div><div><Label>{t("Maximum price (EGP)","أعلى سعر (جنيه)")}</Label><Input className="mt-1" inputMode="decimal" value={filters.maxPrice} onChange={event=>setFilters(current=>({...current,maxPrice:event.target.value}))}/></div><div><Label>{t("Sort","الترتيب")}</Label><select className="mt-1 flex h-10 w-full rounded-md border bg-background px-3 py-2 text-sm" value={filters.sort} onChange={event=>setFilters(current=>({...current,sort:event.target.value}))}><option value="relevance">{t("Relevance","الأكثر صلة")}</option><option value="name">{t("Name","الاسم")}</option><option value="price_high">{t("Highest evidence price","أعلى سعر دليل")}</option><option value="price_low">{t("Lowest evidence price","أقل سعر دليل")}</option><option value="history">{t("Most price history","أكثر تاريخ أسعار")}</option><option value="sources">{t("Most connected sources","أكثر مصادر مترابطة")}</option><option value="offers">{t("Most marketplace offers","أكثر عروض السوق")}</option></select></div><div className="flex flex-col justify-end gap-3 pb-1"><CheckField label={t("Only products with price history","فقط المنتجات ذات تاريخ أسعار")} checked={filters.historyOnly} onChange={checked=>setFilters(current=>({...current,historyOnly:checked}))}/><CheckField label={t("Only verified source or company products","فقط المنتجات الموثقة من مصدر أو شركة")} checked={filters.verifiedOnly} onChange={checked=>setFilters(current=>({...current,verifiedOnly:checked}))}/><CheckField label={t("Only products with approved marketplace offers","فقط المنتجات ذات عروض سوق معتمدة")} checked={filters.offersOnly} onChange={checked=>setFilters(current=>({...current,offersOnly:checked}))}/></div><div className="md:col-span-2 xl:col-span-4"><Button onClick={()=>void load(0)} disabled={loading}>{t("Apply filters","تطبيق الفلاتر")}</Button></div></div>}{error&&<Alert variant="destructive" className="mt-4"><AlertCircle className="h-4 w-4"/><AlertDescription>{error}</AlertDescription></Alert>}</section>

    <section className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{medicines.map(medicine=>{const title=language==="ar"?medicine.name_ar||medicine.name_en||`#${medicine.canonical_id}`:medicine.name_en||medicine.name_ar||`#${medicine.canonical_id}`;const subtitle=language==="ar"?medicine.name_en:medicine.name_ar;const currentPrice=formatPrice(medicine.current_price_egp,medicine.price_currency||"EGP");const range=medicine.min_price_egp!=null&&medicine.max_price_egp!=null&&medicine.min_price_egp!==medicine.max_price_egp?`${Number(medicine.min_price_egp).toLocaleString()}–${Number(medicine.max_price_egp).toLocaleString()} EGP`:null;return <Card key={medicine.canonical_id} className="h-full overflow-hidden shadow-sm transition hover:-translate-y-.5 hover:shadow-md">{medicine.image_url&&<img src={medicine.image_url} alt={title} className="h-40 w-full bg-muted/30 object-contain p-3" loading="lazy"/>}<CardHeader><div className="flex flex-wrap items-start justify-between gap-3"><div><CardTitle className="text-lg leading-7"><a href={`/catalog/${medicine.canonical_id}`} className="hover:text-primary">{title}</a></CardTitle>{subtitle&&<p className="mt-1 text-sm text-muted-foreground">{subtitle}</p>}</div><div className="flex flex-col gap-1">{medicine.has_verified_dataset&&<Badge className="gap-1"><ShieldCheck className="h-3 w-3"/>{t("Dataset verified","بيانات موثقة")}</Badge>}{medicine.has_company_verified_source&&<Badge variant="secondary" className="gap-1"><Building2 className="h-3 w-3"/>{t("Company verified","شركة موثقة")}</Badge>}</div></div></CardHeader><CardContent className="space-y-3 text-sm"><div className="flex flex-wrap gap-2">{currentPrice&&<Badge>{currentPrice}</Badge>}{medicine.has_price_history&&<Badge variant="secondary"><History className="mr-1 h-3 w-3"/>{medicine.distinct_price_count} {t("prices","أسعار")}</Badge>}{medicine.marketplace_offer_count>0&&<Badge variant="outline"><Store className="mr-1 h-3 w-3"/>{medicine.marketplace_offer_count} {t("offers","عروض")}</Badge>}{medicine.route&&<Badge variant="outline">{medicine.route}</Badge>}</div>{range&&<Info label={t("Observed evidence range","نطاق أسعار الأدلة")} value={range}/>}<Info label={t("Scientific name","الاسم العلمي")} value={medicine.scientific_name}/><Info label={t("Manufacturer","الشركة المصنعة")} value={medicine.manufacturer}/>{medicine.marketplace_offer_count>0&&<div className="rounded-lg border bg-primary/5 p-3"><div className="text-xs text-muted-foreground">{t("Lowest approved marketplace offer","أقل عرض سوق معتمد")}</div><div className="font-bold">{formatPrice(medicine.lowest_marketplace_price_egp)}</div><div className="mt-1 text-xs text-muted-foreground">{medicine.marketplace_seller_count} {t("verified sellers","بائع موثق")}</div></div>}<div className="flex flex-wrap gap-2">{medicine.source_systems.map(source=><Badge key={source} variant="outline">{sourceLabel(source)}</Badge>)}</div><div className="flex flex-wrap gap-2"><Button asChild size="sm"><a href={`/catalog/${medicine.canonical_id}`}>{t("Product page","صفحة المنتج")}</a></Button>{medicine.marketplace_offer_count>0&&<Button asChild size="sm" variant="outline"><a href={`/marketplace?q=${encodeURIComponent(title)}`}>{t("Compare offers","قارن العروض")}</a></Button>}</div></CardContent></Card>;})}{!loading&&medicines.length===0&&<Card><CardContent className="p-8 text-center text-sm text-muted-foreground">{t("No medicines match the selected search and filters.","لا توجد أدوية تطابق البحث والفلاتر المحددة.")}</CardContent></Card>}</section>

    {total>PAGE_SIZE&&<section className="mt-6 flex flex-wrap items-center justify-between gap-3 rounded-xl border bg-card p-4"><div className="text-sm text-muted-foreground">{t("Page","صفحة")} {page.toLocaleString()} / {pages.toLocaleString()}</div><div className="flex gap-2"><Button variant="outline" disabled={loading||offset===0} onClick={()=>void load(Math.max(0,offset-PAGE_SIZE))}>{t("Previous","السابق")}</Button><Button variant="outline" disabled={loading||offset+PAGE_SIZE>=total} onClick={()=>void load(offset+PAGE_SIZE)}>{t("Next","التالي")}</Button></div></section>}

    <section className="mt-10 rounded-3xl border bg-muted/30 p-6 md:p-8"><div className="grid gap-6 lg:grid-cols-[1fr_auto] lg:items-center"><div><p className="text-sm font-semibold uppercase tracking-wide text-primary">{t("Connected participation","مشاركة مترابطة")}</p><h2 className="mt-2 text-3xl font-bold">{t("Improve the encyclopedia, publish company products, or offer verified supply","طوّر الموسوعة أو انشر منتجات الشركات أو قدم توريدًا موثقًا")}</h2><p className="mt-3 max-w-3xl text-muted-foreground">{t("Medicine companies contribute new portfolio products through evidence review. Licensed sellers create verified shops and offers. Healthcare buyers compare evidence and request quotations without anonymous checkout.","تضيف شركات الأدوية منتجات محافظها عبر مراجعة الأدلة، وينشئ البائعون المرخصون متاجر وعروضًا موثقة، ويقارن المشترون الأدلة ويطلبون عروض أسعار دون شراء مجهول.")}</p></div><div className="flex flex-col gap-2 sm:flex-row lg:flex-col"><a href="/industry" className="rounded-lg border bg-background px-5 py-3 text-center text-sm font-semibold"><Building2 className="mr-2 inline h-4 w-4"/>{t("Company contributions","مساهمات الشركات")}</a><a href="/marketplace" className="rounded-lg bg-primary px-5 py-3 text-center text-sm font-semibold text-primary-foreground"><Store className="mr-2 inline h-4 w-4"/>{t("Medicine marketplace","سوق الأدوية")}</a></div></div></section>
  </main>;
}

function Metric({label,value}:{label:string;value:number}){return <Card><CardContent className="p-4"><div className="text-xs text-muted-foreground">{label}</div><div className="mt-1 text-2xl font-bold">{value.toLocaleString()}</div></CardContent></Card>;}
function ValueCard({icon:Icon,title,text}:{icon:typeof BookOpen;title:string;text:string}){return <Card className="border-primary/15"><CardContent className="flex gap-3 p-4"><div className="rounded-xl bg-primary/10 p-2 text-primary"><Icon className="h-5 w-5"/></div><div><div className="font-semibold">{title}</div><p className="mt-1 text-sm leading-6 text-muted-foreground">{text}</p></div></CardContent></Card>;}
function SelectField({label,value,onChange,options,emptyLabel}:{label:string;value:string;onChange:(value:string)=>void;options:Facet[];emptyLabel:string}){return <div><Label>{label}</Label><select className="mt-1 flex h-10 w-full rounded-md border bg-background px-3 py-2 text-sm" value={value} onChange={event=>onChange(event.target.value)}><option value="">{emptyLabel}</option>{options.map(option=><option key={option.facet_value} value={option.facet_value}>{option.facet_value} ({Number(option.product_count).toLocaleString()})</option>)}</select></div>;}
function CheckField({label,checked,onChange}:{label:string;checked:boolean;onChange:(checked:boolean)=>void}){return <label className="flex items-start gap-2 text-sm"><input type="checkbox" className="mt-1 h-4 w-4" checked={checked} onChange={event=>onChange(event.target.checked)}/><span>{label}</span></label>;}
function Info({label,value}:{label:string;value:string|null|undefined}){return <div><div className="text-xs text-muted-foreground">{label}</div><div className="font-medium break-words">{value||"—"}</div></div>;}
