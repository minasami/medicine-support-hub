import { useEffect, useState } from "react";
import { AlertCircle, RefreshCw, UserPlus, Users } from "lucide-react";
import { Alert, AlertDescription } from "@/components/ui/alert";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { usePatientAuth } from "@/lib/patient-auth";

type Branch={id:string;branch_name:string;city:string|null};
type Member={id:string;user_id:string;member_role:"owner"|"manager"|"accountant";is_active:boolean;created_at:string};
type Profile={id:string;full_name:string|null;phone:string|null;role:string;is_active:boolean};

export default function PharmacyMembers(){
 const {isAuthenticated,session,supabaseFetch}=usePatientAuth();
 const userId=session?.user?.id;
 const [branches,setBranches]=useState<Branch[]>([]);
 const [branchId,setBranchId]=useState("");
 const [members,setMembers]=useState<Member[]>([]);
 const [candidates,setCandidates]=useState<Profile[]>([]);
 const [accountantId,setAccountantId]=useState("");
 const [role,setRole]=useState<"accountant"|"manager">("accountant");
 const [loading,setLoading]=useState(true);
 const [saving,setSaving]=useState(false);
 const [error,setError]=useState<string|null>(null);
 const [message,setMessage]=useState<string|null>(null);
 async function load(){setLoading(true);setError(null);try{if(!isAuthenticated||!userId)throw new Error("Sign in first.");const rows=await supabaseFetch<Branch[]>("/rest/v1/pharmacy_branches?select=id,branch_name,city&is_active=eq.true&order=created_at.asc");setBranches(rows);const active=branchId||rows[0]?.id||"";setBranchId(active);const profileRows=await supabaseFetch<Profile[]>("/rest/v1/profiles?select=id,full_name,phone,role,is_active&role=in.(pharmacy_accountant,branch_manager)&is_active=eq.true&order=full_name.asc&limit=200");setCandidates(profileRows);if(!active){setMembers([]);return}const memberRows=await supabaseFetch<Member[]>(`/rest/v1/pharmacy_branch_members?select=id,user_id,member_role,is_active,created_at&branch_id=eq.${active}&order=created_at.asc`);setMembers(memberRows)}catch(cause){setError(cause instanceof Error?cause.message:"Could not load pharmacy members.")}finally{setLoading(false)}}
 useEffect(()=>{void load()},[isAuthenticated,userId,branchId]);
 async function addMember(){if(!branchId||!accountantId.trim())return;setSaving(true);setError(null);setMessage(null);try{await supabaseFetch("/rest/v1/pharmacy_branch_members",{method:"POST",headers:{Prefer:"resolution=merge-duplicates"},body:JSON.stringify({branch_id:branchId,user_id:accountantId.trim(),member_role:role,is_active:true})});setAccountantId("");setMessage(role==="accountant"?"Pharmacy accountant can now access this branch finance module.":"Manager linked to branch.");await load()}catch(cause){setError(cause instanceof Error?cause.message:"Could not link member. Make sure the selected user exists and you are the branch owner or manager.")}finally{setSaving(false)}}
 async function deactivateMember(member:Member){if(member.member_role==="owner")return;setSaving(true);setError(null);setMessage(null);try{await supabaseFetch(`/rest/v1/pharmacy_branch_members?id=eq.${member.id}`,{method:"PATCH",body:JSON.stringify({is_active:false})});setMessage("Member access removed.");await load()}catch(cause){setError(cause instanceof Error?cause.message:"Could not remove access.")}finally{setSaving(false)}}
 return <div className="container mx-auto max-w-4xl px-4 py-8"><div className="mb-6 flex items-start justify-between gap-3"><div><div className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">Pharmacy access</div><h1 className="mt-2 flex items-center gap-2 text-3xl font-bold"><Users className="h-7 w-7"/>Branch finance access</h1><p className="text-muted-foreground">Link pharmacy accountants and managers to a branch so they can use Pharmacy Finance.</p></div><Button variant="outline" onClick={()=>void load()} disabled={loading}><RefreshCw className="mr-2 h-4 w-4"/>Refresh</Button></div>{error&&<Alert variant="destructive" className="mb-4"><AlertCircle className="h-4 w-4"/><AlertDescription>{error}</AlertDescription></Alert>}{message&&<Alert className="mb-4"><AlertDescription>{message}</AlertDescription></Alert>}<Card className="mb-6"><CardHeader><CardTitle>Current branch</CardTitle></CardHeader><CardContent><select className="flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm" value={branchId} onChange={e=>setBranchId(e.target.value)}>{branches.map(b=><option key={b.id} value={b.id}>{b.branch_name}{b.city?` - ${b.city}`:""}</option>)}</select>{!branches.length&&<p className="mt-3 text-sm text-muted-foreground">Create a branch from Pharmacy Finance first.</p>}</CardContent></Card><Card className="mb-6"><CardHeader><CardTitle className="flex items-center gap-2"><UserPlus className="h-5 w-5"/>Add pharmacy finance user</CardTitle></CardHeader><CardContent className="grid gap-3 sm:grid-cols-[1fr_180px_auto]"><div><Label>Registered pharmacy user</Label><select className="flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm" value={accountantId} onChange={e=>setAccountantId(e.target.value)}><option value="">Select pharmacy accountant or manager</option>{candidates.map(profile=><option key={profile.id} value={profile.id}>{profile.full_name||profile.phone||profile.id} — {profile.role}</option>)}</select><Input className="mt-2" value={accountantId} onChange={e=>setAccountantId(e.target.value)} placeholder="Or paste user UUID"/></div><div><Label>Branch access</Label><select className="flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm" value={role} onChange={e=>setRole(e.target.value as "accountant"|"manager")}><option value="accountant">Accountant</option><option value="manager">Manager</option></select></div><div className="flex items-end"><Button onClick={()=>void addMember()} disabled={saving||!branchId||!accountantId.trim()}>Link</Button></div></CardContent></Card><Card><CardHeader><CardTitle>Branch members</CardTitle></CardHeader><CardContent className="space-y-2">{members.map(m=><div key={m.id} className="flex items-center justify-between rounded-lg border p-3 text-sm"><div><div className="font-mono text-xs">{m.user_id}</div><div className="text-xs text-muted-foreground">Added {new Date(m.created_at).toLocaleDateString()}</div></div><div className="flex items-center gap-2"><Badge variant={m.is_active?"default":"outline"}>{m.is_active?m.member_role:"inactive"}</Badge>{m.member_role!=="owner"&&m.is_active&&<Button size="sm" variant="outline" onClick={()=>void deactivateMember(m)} disabled={saving}>Remove</Button>}</div></div>)}{!members.length&&<p className="text-sm text-muted-foreground">No members yet.</p>}<p className="pt-3 text-xs text-muted-foreground">Set the user profile role to pharmacy_accountant first, then link them here as branch accountant to grant Pharmacy Finance access for this branch.</p></CardContent></Card></div>}
