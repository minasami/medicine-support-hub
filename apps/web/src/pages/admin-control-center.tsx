import { FormEvent, useEffect, useMemo, useState } from "react";
import { Link } from "wouter";
import { AlertCircle, Bot, Check, Clock3, FileScan, Globe2, HardDrive, Play, RefreshCw, Save, Settings2, ShieldCheck, X } from "lucide-react";
import { Alert, AlertDescription } from "@/components/ui/alert";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { AdminCompanyDirectoryGovernance } from "@/components/admin-company-directory-governance";
import { AdminDuplicateMerger } from "@/components/admin-duplicate-merger";
import { usePatientAuth } from "@/lib/patient-auth";
import { useLanguage } from "@/lib/i18n";

type Setting = { setting_key:string; category:string; label:string; description:string|null; value:unknown; value_type:string; is_public:boolean; updated_at:string };
type Approval = { queue_key:string; label:string; pending_count:number; route:string };
type OcrJob = { id:string; source_name:string; mime_type:string; provider_used:string|null; status:string; review_status:string; page_count:number|null; language_codes:string[]; quality_score:number|null; extracted_text:string|null; error_message:string|null; created_at:string };
type Source = { id:string; entity_type:string; source_name:string; root_url:string; allowed_domain:string; crawl_mode:string; canonical_id:number|null; company_slug:string|null; include_paths:string[]; exclude_paths:string[]; max_pages:number; refresh_interval_hours:number; schedule_enabled:boolean; is_active:boolean; last_status:string|null; next_run_at:string|null };
type CrawlJob = { id:string; source_id:string; mode:string; external_job_id:string|null; status:string; pages_discovered:number; pages_processed:number; error_message:string|null; created_at:string };
type Candidate = { id:string; entity_type:string; canonical_id:number|null; company_slug:string|null; source_url:string; source_title:string|null; extracted_data:Record<string,unknown>; confidence_score:number; status:string; created_at:string };
type IntegrationStatus = { google_document_ai:{configured:boolean;provider:string}; firecrawl:{configured:boolean;automatic_sync_ready:boolean;api_version:string}; image_search:{configured:boolean}; cron:{configured:boolean}; service_role:{configured:boolean}; security:{secrets_exposed_to_browser:boolean;automatic_publication:boolean;human_review_required:boolean} };
type StorageHealth = { database_bytes:number; quota_bytes:number; usage_percent:number; remaining_bytes:number; status:"healthy"|"warning"|"critical"; checked_at:string };

const SETTINGS_ORDER=["identity","experience","search","marketplace","industry","learning","ocr","firecrawl","approvals","security","operations"];
const emptySource={entity_type:"medicine",source_name:"",root_url:"",crawl_mode:"scrape",canonical_id:"",company_slug:"",include_paths:"",exclude_paths:"",max_pages:"25",refresh_interval_hours:"24",schedule_enabled:false};

export default function AdminControlCenter(){
  const { t } = useLanguage();
  const{session,supabaseFetch}=usePatientAuth();
  const[settings,setSettings]=useState<Setting[]>([]);const[drafts,setDrafts]=useState<Record<string,string>>({});
  const[approvals,setApprovals]=useState<Approval[]>([]);const[ocrJobs,setOcrJobs]=useState<OcrJob[]>([]);
  const[sources,setSources]=useState<Source[]>([]);const[crawlJobs,setCrawlJobs]=useState<CrawlJob[]>([]);const[candidates,setCandidates]=useState<Candidate[]>([]);
  const[integrations,setIntegrations]=useState<IntegrationStatus|null>(null);const[sourceDraft,setSourceDraft]=useState(emptySource);
  const[storageHealth,setStorageHealth]=useState<StorageHealth|null>(null);
  const[ocrFile,setOcrFile]=useState<File|null>(null);const[ocrSourceName,setOcrSourceName]=useState("");
  const[loading,setLoading]=useState(true);const[busy,setBusy]=useState<string|null>(null);const[error,setError]=useState<string|null>(null);const[message,setMessage]=useState<string|null>(null);

  const grouped=useMemo(()=>SETTINGS_ORDER.map(category=>({category,rows:settings.filter(row=>row.category===category)})).filter(group=>group.rows.length),[settings]);
  const pendingTotal=approvals.reduce((sum,row)=>sum+Number(row.pending_count||0),0);

  async function server<T>(path:string,init:RequestInit={}){if(!session?.access_token)throw new Error(t("Platform-admin session required.", "جلسة مشرف المنصة مطلوبة."));const response=await fetch(path,{...init,headers:{Authorization:`Bearer ${session.access_token}`,"Content-Type":"application/json",...(init.headers||{})}});const data=await response.json();if(!response.ok)throw new Error(data?.message||t("Request failed.", "فشل الطلب."));return data as T;}
  async function load(){setLoading(true);setError(null);try{
    if(!session?.access_token)throw new Error(t("Sign in through the staff portal first.", "سجّل الدخول عبر بوابة الموظفين أولًا."));
    const[settingRows,approvalRows,ocrRows,sourceRows,jobRows,candidateRows,status,storageRows]=await Promise.all([
      supabaseFetch<Setting[]>("/rest/v1/platform_settings?select=setting_key,category,label,description,value,value_type,is_public,updated_at&order=category.asc,setting_key.asc"),
      supabaseFetch<Approval[]>("/rest/v1/platform_approval_summary_v1?select=queue_key,label,pending_count,route&order=label.asc"),
      supabaseFetch<OcrJob[]>("/rest/v1/document_ocr_jobs?select=id,source_name,mime_type,provider_used,status,review_status,page_count,language_codes,quality_score,extracted_text,error_message,created_at&order=created_at.desc&limit=30"),
      supabaseFetch<Source[]>("/rest/v1/web_ingestion_sources?select=id,entity_type,source_name,root_url,allowed_domain,crawl_mode,canonical_id,company_slug,include_paths,exclude_paths,max_pages,refresh_interval_hours,schedule_enabled,is_active,last_status,next_run_at&order=created_at.desc&limit=100"),
      supabaseFetch<CrawlJob[]>("/rest/v1/web_ingestion_jobs?select=id,source_id,mode,external_job_id,status,pages_discovered,pages_processed,error_message,created_at&order=created_at.desc&limit=50"),
      supabaseFetch<Candidate[]>("/rest/v1/web_ingestion_candidates?select=id,entity_type,canonical_id,company_slug,source_url,source_title,extracted_data,confidence_score,status,created_at&order=created_at.desc&limit=100"),
      server<IntegrationStatus>("/api/admin-integrations"),
      supabaseFetch<StorageHealth[]>("/rest/v1/rpc/database_storage_admin_health",{method:"POST",body:"{}"}),
    ]);
    const safeSettings=Array.isArray(settingRows)?settingRows:[];
    setSettings(safeSettings);setApprovals(Array.isArray(approvalRows)?approvalRows:[]);setOcrJobs(Array.isArray(ocrRows)?ocrRows:[]);
    setSources(Array.isArray(sourceRows)?sourceRows:[]);setCrawlJobs(Array.isArray(jobRows)?jobRows:[]);setCandidates(Array.isArray(candidateRows)?candidateRows:[]);
    setIntegrations(status);setStorageHealth(Array.isArray(storageRows)?storageRows[0]||null:null);
    setDrafts(Object.fromEntries(safeSettings.map(row=>[row.setting_key,settingText(row)])));
  }catch(cause){setError(cause instanceof Error?cause.message:t("Could not load control center.", "تعذّر تحميل مركز التحكم."));}finally{setLoading(false);}}

  useEffect(()=>{void load();},[session?.access_token]);

  async function saveSetting(row:Setting){const text=drafts[row.setting_key]??"";setBusy(row.setting_key);setError(null);setMessage(null);try{const value=parseSetting(row,text);await supabaseFetch(`/rest/v1/platform_settings?setting_key=eq.${row.setting_key}`,{method:"PATCH",headers:{Prefer:"return=minimal"},body:JSON.stringify({value,updated_at:new Date().toISOString()})});setMessage(t(`Saved ${row.label}.`, `تم حفظ ${row.label}.`));await load();}catch(cause){setError(cause instanceof Error?cause.message:t(`Could not save ${row.label}.`, `تعذّر حفظ ${row.label}.`));}finally{setBusy(null);}}
  async function runOcr(event:FormEvent){event.preventDefault();if(!ocrFile)return;setBusy("ocr");setError(null);setMessage(null);try{const base64=await fileBase64(ocrFile);await server("/api/admin-ocr-process",{method:"POST",body:JSON.stringify({source_name:ocrSourceName.trim()||ocrFile.name,mime_type:ocrFile.type,file_base64:base64})});setOcrFile(null);setOcrSourceName("");setMessage(t("Document sent to Google Enterprise Document OCR.", "أُرسل المستند إلى Google Enterprise Document OCR."));await load();}catch(cause){setError(cause instanceof Error?cause.message:t("Could not run OCR.", "تعذّر تشغيل OCR."));}finally{setBusy(null);}}
  async function reviewOcr(job:OcrJob,review_status:"approved"|"rejected"){setBusy(job.id);setError(null);try{await supabaseFetch(`/rest/v1/document_ocr_jobs?id=eq.${job.id}`,{method:"PATCH",headers:{Prefer:"return=minimal"},body:JSON.stringify({review_status,updated_at:new Date().toISOString()})});setMessage(t(`OCR job ${review_status}.`, `مهمة OCR: ${review_status}.`));await load();}catch(cause){setError(cause instanceof Error?cause.message:t("Could not review OCR job.", "تعذّر مراجعة مهمة OCR."));}finally{setBusy(null);}}
  async function createSource(event:FormEvent){event.preventDefault();setBusy("source");setError(null);setMessage(null);try{await supabaseFetch("/rest/v1/web_ingestion_sources",{method:"POST",headers:{Prefer:"return=minimal"},body:JSON.stringify({entity_type:sourceDraft.entity_type,source_name:sourceDraft.source_name.trim(),root_url:sourceDraft.root_url.trim(),allowed_domain:new URL(sourceDraft.root_url.trim()).hostname,crawl_mode:sourceDraft.crawl_mode,canonical_id:sourceDraft.canonical_id?Number(sourceDraft.canonical_id):null,company_slug:sourceDraft.company_slug.trim()||null,include_paths:lines(sourceDraft.include_paths),exclude_paths:lines(sourceDraft.exclude_paths),max_pages:Number(sourceDraft.max_pages||25),refresh_interval_hours:Number(sourceDraft.refresh_interval_hours||24),schedule_enabled:sourceDraft.schedule_enabled,is_active:true})});setSourceDraft(emptySource);setMessage(t("Ingestion source created.", "تم إنشاء مصدر الاستيراد."));await load();}catch(cause){setError(cause instanceof Error?cause.message:t("Could not create source.", "تعذّر إنشاء المصدر."));}finally{setBusy(null);}}
  async function runSource(source:Source){setBusy(source.id);setError(null);setMessage(null);try{const result=await server<{mode:string;pages_processed?:number;candidates_created?:number}>("/api/admin-firecrawl-sync",{method:"POST",body:JSON.stringify({source_id:source.id,mode:source.crawl_mode})});setMessage(t(`Ingestion started (${result.mode}).`, `بدأ الاستيراد (${result.mode}).`));await load();}catch(cause){setError(cause instanceof Error?cause.message:t("Could not run ingestion source.", "تعذّر تشغيل مصدر الاستيراد."));}finally{setBusy(null);}}
  async function toggleSource(source:Source,field:"is_active"|"schedule_enabled"){setBusy(source.id);setError(null);try{await supabaseFetch(`/rest/v1/web_ingestion_sources?id=eq.${source.id}`,{method:"PATCH",headers:{Prefer:"return=minimal"},body:JSON.stringify({[field]:!source[field],updated_at:new Date().toISOString()})});await load();}catch(cause){setError(cause instanceof Error?cause.message:t("Could not update source.", "تعذّر تحديث المصدر."));}finally{setBusy(null);}}
  async function pollJob(job:CrawlJob){setBusy(job.id);setError(null);try{await server("/api/admin-firecrawl-sync",{method:"POST",body:JSON.stringify({job_id:job.id,mode:"poll"})});await load();}catch(cause){setError(cause instanceof Error?cause.message:t("Could not poll job status.", "تعذّر استطلاع حالة المهمة."));}finally{setBusy(null);}}
  async function reviewCandidate(candidate:Candidate,status:"approved"|"rejected"){setBusy(candidate.id);setError(null);try{await supabaseFetch(`/rest/v1/web_ingestion_candidates?id=eq.${candidate.id}`,{method:"PATCH",headers:{Prefer:"return=minimal"},body:JSON.stringify({status,updated_at:new Date().toISOString()})});setMessage(t(`Candidate ${status}.`, `المرشح: ${status}.`));await load();}catch(cause){setError(cause instanceof Error?cause.message:t("Could not review candidate.", "تعذّر مراجعة المرشح."));}finally{setBusy(null);}}

  if(loading)return <main className="container mx-auto max-w-7xl p-8"><div className="flex items-center justify-center p-12 text-muted-foreground"><RefreshCw className="mr-2 h-5 w-5 animate-spin"/>{t("Loading control center…", "جاري تحميل مركز التحكم…")}</div></main>;

  return <main className="container mx-auto max-w-7xl px-4 py-8">
    <div className="flex flex-wrap items-center justify-between gap-4 rounded-3xl border bg-card p-6 shadow-sm md:p-8">
      <div>
        <div className="flex items-center gap-2 text-sm font-semibold uppercase tracking-wide text-primary"><ShieldCheck className="h-4 w-4"/>{t("Platform-admin control center", "مركز تحكم مشرف المنصة")}</div>
        <h1 className="mt-1 text-3xl font-bold tracking-tight md:text-4xl">{t("System settings, integrations, and automation", "إعدادات النظام والتكاملات والأتمتة")}</h1>
        <p className="mt-2 text-sm text-muted-foreground">{t("Manage platform parameters, review queues, enterprise OCR, Firecrawl scraping sources, database health, and security posture.", "أدِر معاملات المنصة وطوابير المراجعة وOCR المؤسسي ومصادر Firecrawl وصحة قاعدة البيانات ووضع الأمان.")}</p>
      </div>
      <Button variant="outline" onClick={()=>void load()}><RefreshCw className="mr-2 h-4 w-4"/>{t("Refresh", "تحديث")}</Button>
    </div>

    {error&&<Alert variant="destructive" className="mt-5"><AlertCircle className="h-4 w-4"/><AlertDescription>{error}</AlertDescription></Alert>}
    {message&&<Alert className="mt-5"><Check className="h-4 w-4 text-emerald-600"/><AlertDescription>{message}</AlertDescription></Alert>}

    {integrations&&<>
      <section className="mt-6 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">
        <Status label={t("Google Enterprise OCR", "OCR جوجل المؤسسي")} value={integrations.google_document_ai.configured?t("Ready", "جاهز"):t("Key missing", "المفتاح مفقود")} ready={integrations.google_document_ai.configured}/>
        <Status label={t("Firecrawl Ingestion", "استيراد Firecrawl")} value={integrations.firecrawl.configured?t("Configured", "مُعد"):t("Key missing", "المفتاح مفقود")} ready={integrations.firecrawl.configured}/>
        <Status label={t("Open approval tasks", "مهام الموافقة المفتوحة")} value={pendingTotal} ready={pendingTotal===0}/>
        <Status label={t("Storage usage", "استخدام التخزين")} value={storageHealth?`${Number(storageHealth.usage_percent).toFixed(1)}%`:t("Checking", "جاري الفحص")} ready={Boolean(!storageHealth||storageHealth.status==="healthy")}/>
      </section>

      {storageHealth&&storageHealth.status!=="healthy"&&<Alert variant={storageHealth.status==="critical"?"destructive":"default"} className="mt-5"><HardDrive className="h-4 w-4"/><AlertDescription>{t("Database storage is", "تخزين قاعدة البيانات")} {Number(storageHealth.usage_percent).toFixed(1)}% {t("full with", "ممتلئ مع")} {formatBytes(storageHealth.remaining_bytes)} {t("remaining.", "متبقية.")}</AlertDescription></Alert>}

      <Section icon={ShieldCheck} title={t("Approval command center", "مركز قيادة الموافقات")} description={t("Every operational and content trust queue remains visible from one place.", "كل طوابير التشغيل والثقة في المحتوى ظاهرة من مكان واحد.")}><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-5">{approvals.map(row=><Link key={row.queue_key} href={row.route} className="rounded-xl border p-4 transition hover:border-primary hover:bg-primary/5"><div className="text-3xl font-bold">{Number(row.pending_count).toLocaleString()}</div><div className="mt-1 font-semibold">{row.label}</div><div className="mt-2 text-xs text-muted-foreground">{t("Open queue", "فتح الطابور")}</div></Link>)}</div></Section>

      <AdminDuplicateMerger />

      <Section icon={Settings2} title={t("Platform settings and customizations", "إعدادات المنصة والتخصيصات")} description={t("Values are audited. Secret-like settings stay in server environment variables.", "تُدقّق القيم. الإعدادات الشبيهة بالأسرار تبقى في متغيرات بيئة الخادم.")}><div className="space-y-6">{grouped.map(group=><div key={group.category}><h3 className="mb-3 text-lg font-semibold capitalize">{group.category}</h3><div className="grid gap-3 lg:grid-cols-2">{group.rows.map(row=><Card key={row.setting_key}><CardContent className="space-y-3 p-5"><div className="flex items-start justify-between gap-3"><div><div className="font-semibold">{row.label}</div><div className="mt-1 text-xs text-muted-foreground">{row.setting_key}{row.is_public?` · ${t("public", "عام")}`:` · ${t("private", "خاص")}`}</div></div><Badge variant={row.is_public?"default":"outline"}>{row.value_type}</Badge></div>{row.description&&<p className="text-sm text-muted-foreground">{row.description}</p>}<SettingInput row={row} value={drafts[row.setting_key]??""} onChange={value=>setDrafts(current=>({...current,[row.setting_key]:value}))}/><Button size="sm" onClick={()=>void saveSetting(row)} disabled={busy===row.setting_key}><Save className="mr-2 h-4 w-4"/>{t("Save", "حفظ")}</Button></CardContent></Card>)}</div></div>)}</div></Section>

      <Section icon={FileScan} title={t("Enterprise document OCR", "OCR مستندات مؤسسي")} description={t("Google Enterprise Document OCR is the primary provider. Only extracted text and review status are stored.", "Google Enterprise Document OCR هو الموفر الأساسي. يُخزَّن النص المستخرج وحالة المراجعة فقط.")}><form onSubmit={runOcr} className="grid gap-3 rounded-xl border bg-muted/20 p-4 md:grid-cols-[1fr_1fr_auto] md:items-end"><div><Label>{t("Document label", "تسمية المستند")}</Label><Input className="mt-1" value={ocrSourceName} onChange={event=>setOcrSourceName(event.target.value)} placeholder={t("Medicine leaflet…", "نشرة دواء…")}/></div><div><Label>{t("PDF or image", "PDF أو صورة")}</Label><Input className="mt-1" type="file" accept="application/pdf,image/jpeg,image/png,image/tiff,image/webp" onChange={event=>setOcrFile(event.target.files?.[0]||null)} required/></div><Button type="submit" disabled={!ocrFile||busy==="ocr"||!integrations?.google_document_ai.configured}><FileScan className="mr-2 h-4 w-4"/>{t("Run OCR", "تشغيل OCR")}</Button></form><div className="mt-4 grid gap-3 lg:grid-cols-2">{ocrJobs.map(job=><Card key={job.id}><CardContent className="space-y-3 p-4"><div className="flex items-start justify-between gap-3"><div><div className="font-semibold">{job.source_name}</div><div className="text-xs text-muted-foreground">{new Date(job.created_at).toLocaleString()} · {job.mime_type}</div></div><Badge variant={job.status==="completed"?"default":job.status==="failed"?"destructive":"secondary"}>{job.status}</Badge></div>{job.error_message&&<p className="text-sm text-destructive">{job.error_message}</p>}{job.extracted_text&&<pre className="max-h-40 overflow-auto whitespace-pre-wrap rounded-lg bg-muted p-3 text-xs">{job.extracted_text.slice(0,2500)}</pre>}{job.status==="completed"&&job.review_status==="pending"&&<div className="flex gap-2"><Button size="sm" onClick={()=>void reviewOcr(job,"approved")} disabled={busy===job.id}><Check className="mr-1 h-4 w-4"/>{t("Approve extraction", "اعتماد الاستخراج")}</Button><Button size="sm" variant="destructive" onClick={()=>void reviewOcr(job,"rejected")} disabled={busy===job.id}><X className="mr-1 h-4 w-4"/>{t("Reject", "رفض")}</Button></div>}</CardContent></Card>)}{!ocrJobs.length&&<Empty text={t("No OCR jobs yet.", "لا توجد مهام OCR بعد.")}/>}</div></Section>

      <Section icon={Globe2} title={t("Firecrawl medicine and company sources", "مصادر Firecrawl للأدوية والشركات")} description={t("Create an allow-listed HTTPS source and schedule controlled refreshes.", "أنشئ مصدر HTTPS مسموحًا وجدول تحديثات محكومة.")}><form onSubmit={createSource} className="grid gap-3 rounded-xl border bg-muted/20 p-4 md:grid-cols-2 xl:grid-cols-4"><Field label={t("Entity type", "نوع الكيان")}><select className="h-10 w-full rounded-md border bg-background px-3" value={sourceDraft.entity_type} onChange={event=>setSourceDraft({...sourceDraft,entity_type:event.target.value})}><option value="medicine">{t("Medicine", "دواء")}</option><option value="company">{t("Company", "شركة")}</option></select></Field><Field label={t("Source name", "اسم المصدر")}><Input value={sourceDraft.source_name} onChange={event=>setSourceDraft({...sourceDraft,source_name:event.target.value})} required/></Field><Field label={t("HTTPS root URL", "رابط الجذر HTTPS")}><Input type="url" value={sourceDraft.root_url} onChange={event=>setSourceDraft({...sourceDraft,root_url:event.target.value})} required/></Field><Field label={t("Mode", "الوضع")}><select className="h-10 w-full rounded-md border bg-background px-3" value={sourceDraft.crawl_mode} onChange={event=>setSourceDraft({...sourceDraft,crawl_mode:event.target.value})}><option value="scrape">{t("Single page scrape", "سحب صفحة واحدة")}</option><option value="crawl">{t("Bounded site crawl", "زحف موقع محدود")}</option></select></Field><Field label={t("Max pages", "أقصى صفحات")}><Input type="number" min="1" max="250" value={sourceDraft.max_pages} onChange={event=>setSourceDraft({...sourceDraft,max_pages:event.target.value})}/></Field><Field label={t("Refresh hours", "ساعات التحديث")}><Input type="number" min="6" value={sourceDraft.refresh_interval_hours} onChange={event=>setSourceDraft({...sourceDraft,refresh_interval_hours:event.target.value})}/></Field><div className="md:col-span-2 xl:col-span-4"><Button type="submit" disabled={busy==="source"}><Globe2 className="mr-2 h-4 w-4"/>{t("Add governed source", "إضافة مصدر محكوم")}</Button></div></form><div className="mt-4 grid gap-3 xl:grid-cols-2">{sources.map(source=><Card key={source.id}><CardContent className="space-y-3 p-4"><div className="flex items-start justify-between gap-3"><div><div className="font-semibold">{source.source_name}</div><a href={source.root_url} target="_blank" rel="noreferrer" className="text-xs text-primary">{source.allowed_domain}</a></div><Badge>{source.entity_type} · {source.crawl_mode}</Badge></div><div className="flex flex-wrap gap-2"><Button size="sm" onClick={()=>void runSource(source)} disabled={busy===source.id||!integrations?.firecrawl.configured}><Play className="mr-1 h-4 w-4"/>{t("Run now", "تشغيل الآن")}</Button><Button size="sm" variant="outline" onClick={()=>void toggleSource(source,"is_active")} disabled={busy===source.id}>{source.is_active?t("Pause", "إيقاف"):t("Activate", "تفعيل")}</Button></div></CardContent></Card>)}{!sources.length&&<Empty text={t("No governed web sources yet.", "لا توجد مصادر ويب محكومة بعد.")}/>}</div></Section>

      <Section icon={Clock3} title={t("Firecrawl jobs and review candidates", "مهام Firecrawl ومرشحو المراجعة")} description={t("Poll crawls, inspect evidence, and promote approved candidates into moderation.", "تابع الزحف، وافحص الأدلة، وارفع المرشحين المعتمدين إلى المراجعة.")}><div className="grid gap-3 lg:grid-cols-2">{crawlJobs.map(job=><Card key={job.id}><CardContent className="p-4"><div className="flex items-start justify-between gap-3"><div><div className="font-semibold">{job.mode} job</div><div className="text-xs text-muted-foreground">{new Date(job.created_at).toLocaleString()}</div></div><Badge>{job.status}</Badge></div>{job.status==="running"&&job.mode==="crawl"&&<Button className="mt-3" size="sm" variant="outline" onClick={()=>void pollJob(job)} disabled={busy===job.id}><RefreshCw className="mr-1 h-4 w-4"/>{t("Poll job", "استطلاع المهمة")}</Button>}</CardContent></Card>)}{!crawlJobs.length&&<Empty text={t("No Firecrawl jobs yet.", "لا توجد مهام Firecrawl بعد.")}/>}</div><div className="mt-6 grid gap-3 xl:grid-cols-2">{candidates.map(candidate=><Card key={candidate.id}><CardContent className="space-y-3 p-4"><div className="flex items-start justify-between gap-3"><div><div className="font-semibold">{candidate.source_title||candidate.entity_type}</div></div><Badge>{candidate.status}</Badge></div>{candidate.status==="pending"&&<div className="flex gap-2"><Button size="sm" onClick={()=>void reviewCandidate(candidate,"approved")} disabled={busy===candidate.id}><Check className="mr-1 h-4 w-4"/>{t("Approve and route", "اعتماد وتوجيه")}</Button><Button size="sm" variant="destructive" onClick={()=>void reviewCandidate(candidate,"rejected")} disabled={busy===candidate.id}><X className="mr-1 h-4 w-4"/>{t("Reject", "رفض")}</Button></div>}</CardContent></Card>)}{!candidates.length&&<Empty text={t("No web-ingestion candidates yet.", "لا يوجد مرشحون للاستيراد بعد.")}/>}</div></Section>

      <Alert className="mt-8"><Bot className="h-4 w-4"/><AlertDescription>{t("OCR and Firecrawl accelerate evidence collection. Human review remains mandatory.", "OCR وFirecrawl يسرّعان جمع الأدلة. المراجعة البشرية تبقى إلزامية.")}</AlertDescription></Alert>
    </>}
  </main>;
}

function Section({icon:Icon,title,description,children}:{icon:typeof Settings2;title:string;description:string;children:React.ReactNode}){return <section className="mt-10"><div className="mb-4"><div className="flex items-center gap-2"><Icon className="h-5 w-5 text-primary"/><h2 className="text-2xl font-bold">{title}</h2></div><p className="mt-1 max-w-4xl text-sm text-muted-foreground">{description}</p></div>{children}</section>;}
function Status({label,value,ready}:{label:string;value:string|number;ready:boolean}){return <Card><CardContent className="p-4"><div className="text-xs text-muted-foreground">{label}</div><div className="mt-1 flex items-center gap-2 text-lg font-bold">{String(value)}<span className={`h-2.5 w-2.5 rounded-full ${ready?"bg-emerald-500":"bg-amber-500"}`}/></div></CardContent></Card>;}
function Field({label,children}:{label:string;children:React.ReactNode}){return <div><Label>{label}</Label><div className="mt-1">{children}</div></div>;}
function Empty({text}:{text:string}){return <Card><CardContent className="p-6 text-sm text-muted-foreground">{text}</CardContent></Card>;}
function settingText(row:Setting){if(row.value_type==="json")return JSON.stringify(row.value,null,2);if(row.value_type==="boolean")return row.value===true?"true":"false";return String(row.value??"");}
function parseSetting(row:Setting,value:string){if(row.value_type==="boolean")return value==="true";if(row.value_type==="integer"){const parsed=Number.parseInt(value,10);if(!Number.isFinite(parsed))throw new Error(`${row.label} must be an integer.`);return parsed;}if(row.value_type==="number"){const parsed=Number(value);if(!Number.isFinite(parsed))throw new Error(`${row.label} must be a number.`);return parsed;}if(row.value_type==="json")return JSON.parse(value);return value;}
function SettingInput({row,value,onChange}:{row:Setting;value:string;onChange:(value:string)=>void}){if(row.value_type==="boolean")return <select className="h-10 w-full rounded-md border bg-background px-3" value={value} onChange={event=>onChange(event.target.value)}><option value="true">Enabled / مفعّل</option><option value="false">Disabled / معطّل</option></select>;if(row.value_type==="json")return <Textarea value={value} onChange={event=>onChange(event.target.value)} className="font-mono text-xs"/>;return <Input type={["integer","number"].includes(row.value_type)?"number":"text"} value={value} onChange={event=>onChange(event.target.value)}/>;}
function settingValue(rows:Setting[],key:string,fallback:unknown){return rows.find(row=>row.setting_key===key)?.value??fallback;}
function lines(value:string){return value.split(/\r?\n|,/).map(item=>item.trim()).filter(Boolean);}
function fileBase64(file:File){return new Promise<string>((resolve,reject)=>{const reader=new FileReader();reader.onerror=()=>reject(new Error("Could not read file."));reader.onload=()=>resolve(String(reader.result||"").split(",")[1]||"");reader.readAsDataURL(file);});}
function formatBytes(value:number){if(value>=1024**3)return `${(value/1024**3).toFixed(1)} GB`;if(value>=1024**2)return `${(value/1024**2).toFixed(1)} MB`;return `${Math.max(0,value/1024).toFixed(1)} KB`;}
